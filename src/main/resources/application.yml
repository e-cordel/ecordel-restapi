app.xilogravura.path: "./"

ftp:
  server:
  port:
  user:
  password:
  directory:

spring:
  jpa:
    properties:
      hibernate.jdbc.lob.non_contextual_creation: true
    hibernate:
      ddl-auto: none